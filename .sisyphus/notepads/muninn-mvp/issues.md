## 2026-04-11
- `lsp_diagnostics` could not run on CSS because Biome is not installed in this environment.
- `npx supabase gen types` could not run here because no `SUPABASE_ACCESS_TOKEN` is configured; types were added manually from the known schema instead.
